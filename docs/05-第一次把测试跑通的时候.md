# 第一次把测试跑通的时候

第一次看到终端里出现 `passed`，其实没有想象中那么激动，但会觉得这件事终于不是停在代码里了。之前总觉得自动化测试离自己很远，安装环境、浏览器、命令行，每一步都可能卡住。

那天前面其实失败了好几次。有一次是浏览器没装好，有一次是页面地址写错，还有一次只是测试文件名字不符合 pytest 的规则。现在回头看，这些都不算复杂的问题，但当时每个问题都要查一遍。

后来发现，跑通一次以后，第二次就没那么可怕了。很多学习都是这样，先让它动起来，再慢慢理解。能看到一个完整结果，比只看教程里的代码更容易记住。
